'use strict';

/**
 * available-categorie service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::available-categorie.available-categorie');
